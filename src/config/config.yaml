data_ingestion:
  raw_data_path: data/raw/data.csv

data_validation:
  schema_path: config/schema.yaml

data_cleaning:
  remove_duplicates: true
  fill_missing: mean

data_transformation:
  scaling: standard
  encoding: onehot